#include <stdio.h>

int main()
{
    // Constants.
    const float PI = 3.14159;
    printf("PI = %f", PI);

    /*
    1. Constants cannot be changed.
    2. Name of a constant variable should be in uppercase (it is not necessary but a good practice).
    */

    return 0;
}
